# #4 stepped_shaft ★★★

阶梯轴(三段同轴):Φ10×20 → Φ16×30 → Φ12×15,从 Z=0 沿 +Z 拼接。各段过渡处加 R1 圆角(外凸面)。中央段(Φ16)开 1 条键槽:宽 5 mm 深 3 mm 长 20 mm,沿 +X 方向居中(键槽中心 Z=35)。两端面各倒角 0.5×45°。导出 STEP。

**验收**
- BRep + STEP reimport + solid_count=1 + manifold
- bbox = [16.0, 16.0, 65.0]
- 键槽底面中心 ≈ (5, 0, 35) ±0.1 mm
