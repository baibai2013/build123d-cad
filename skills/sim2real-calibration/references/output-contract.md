# Output Contract

`compare_logs.py` writes:

- `reports/sim2real_calibration.json`
- `reports/sim2real_report.md`

`propose_parameter_update.py` writes:

- `reports/parameter_update.yaml`

`valid: false` means the digital twin should not be trusted for prototype release
until calibration blockers are addressed.
