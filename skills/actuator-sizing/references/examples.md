# Examples

Run the MVP example:

```bash
python skills/actuator-sizing/scripts/estimate_torque.py skills/actuator-sizing/examples/quadruped_mvp
```

The example intentionally uses a borderline actuator so the report can demonstrate
blocker behavior.
