# Examples

Example project:

```text
skills/firmware/examples/quadruped_mvp/
  firmware_plan.yaml
  firmware/
    project_manifest.json
    can_frames.md
    calibration.json
  reports/
    firmware_report.json
    firmware_test_report.json
```

The bundled example intentionally misses one safety/calibration item so blocker
behavior is visible.
